<h1>Your Profile</h1>

<% if (user) { %>
  <div class="feature" style="max-width:520px;">
    <p><strong>Email:</strong> <%= user.email %></p>
    <% if (user.name) { %>
      <p><strong>Name:</strong> <%= user.name %></p>
    <% } %>
  </div>
<% } else { %>
  <p>You’re not signed in. <a href="/users/login">Login</a></p>
<% } %>
